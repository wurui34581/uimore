export const ip = {
    ip_1: window.localStorage['ips']
};
/*export const port = {
    port_1: window.localStorage['port']
};*/
export const version = {
    version_1: window.localStorage['version']
};
